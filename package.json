{
    "name": "sc-simulator",
    "version": "1.0.0",
    "description": "Smart contracts simulator and debugger for signum network",
    "main": "src/index.ts",
    "private": true,
    "homepage": "https://github.com/deleterium/SC-Simulator",
    "scripts": {
        "dev": "npx tsc && npx light-server -s . -p 7000 --no-reload",
        "start": "npx light-server -s . -p 7000 --no-reload",
        "build": "npx tsc"
    },
    "devDependencies": {
        "typescript": "^4.3.5",
        "light-server": "^2.9.1"
    }
}

